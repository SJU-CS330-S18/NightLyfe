<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.csbsju.nightlyfe">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/DarkTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Homescreen"
            android:label="@string/title_activity_homescreen" />
        <activity
            android:name=".AdminHomescreen"
            android:label="@string/title_activity_admin_homescreen" />
        <activity
            android:name=".OwnerHomescreen"
            android:label="@string/title_activity_owner_homescreen" />
        <activity
            android:name=".Restaurant_Page"
            android:label="@string/title_activity_findbars" />
        <activity
            android:name=".Register"
            android:label="@string/title_activity_register" />
        <activity
            android:name=".FriendsList"
            android:label="@string/title_activity_friends" />
        <activity
            android:name=".AccountActivity"
            android:label="@string/title_activity_account" />
        <activity android:name=".BulletinBoard" />
        <activity
            android:name=".FriendSearch"
            android:label="@string/title_activity_friends" />
        <activity
            android:name=".FavoritesList"
            android:label="@string/title_activity_favorites" />
        <activity
            android:name=".GroupsList"
            android:label="@string/title_activity_groups" />
        <activity
            android:name=".NewGroupPage"
            android:label="@string/title_activity_groups" />
        <activity
            android:name=".Group"
            android:label="@string/title_activity_groups" />
        <activity
            android:name=".AddMembers"
            android:label="@string/title_activity_groups" />
        <activity
            android:name=".GroupChat"
            android:label="@string/title_activity_groups" />
        <activity
            android:name=".RestaurantList"
            android:label="@string/title_activity_findbars" />
        <activity
            android:name=".ReviewsActivity"
            android:label="@string/title_activity_reviews" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".BusinessLocation"
            android:label="@string/title_activity_business_location" />
        <activity android:name=".UserDocument" />
        <activity android:name=".AdminReview" />
        <activity android:name=".PremiumRequests" />
        <activity android:name=".OwnerBulletinBoard" />
        <activity android:name=".CreatePoll" />
        <activity android:name=".Poll" />
        <activity android:name=".FriendsMap"></activity>
    </application>

</manifest>